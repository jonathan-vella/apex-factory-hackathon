# B06 spike: GHCP golden path on the dev VM

| Field | Value |
|---|---|
| Date | In progress (started 2026-09-24) |
| Result | Pending: set after run 2 |
| Region | swedencentral |
| Versions | .NET 10 SDK `10.0.401`; GitHub Copilot modernization `1.24.26091501` (marketplace, 2026-09-24); VS Code `1.139.0` on `vm-dev01`. Versions used in each run are in [run1.md](run1.md) and [run2.md](run2.md) |

## Question

Is there a repeatable sequence of GitHub Copilot steps and prompts that takes Contoso University from .NET Framework 4.8 to .NET 10, with uploads on Blob, MSMQ replaced by Service Bus, secrets in Key Vault and OpenTelemetry, running on `vm-dev01` against the source database and packaged as an image in a private ACR, within the C3 (1 hour) and C6 (3 hours) time boxes?

## What we did

1. Deployed the spike services private-only into `rg-spike-b06` with [infra/main.bicep](infra/main.bicep): Storage `stuni<suffix>b06` (container `teaching-materials`), Service Bus Premium `sbns-uni-<suffix>-b06` (queue `notifications`), ACR Premium `cruni<suffix>b06` and Key Vault `kv-uni-<suffix>-b06`. Private endpoints are in `snet-pe-spike` (`10.10.n.128/27`) of `vnet-datacenter`, created by [infra/modules/subnet.bicep](infra/modules/subnet.bicep), and the four private DNS zones in `rg-spike-b06` are linked to `vnet-datacenter`. The owner has Storage Blob Data Contributor, Azure Service Bus Data Sender and Receiver, AcrPush and Key Vault Secrets Officer.
2. Checked from `vm-dev01` with a run command that every hostname resolves to `snet-pe-spike` and that TCP 443 (and 5671 for Service Bus) connects.
3. The owner ran the [protocol](protocol.md) twice, on the branches `spike/b06-run1` and `spike/b06-run2`, committing after each step with manual fixes in the commit body, a chat export per step in `chats/` and the tool versions in `runN-versions.txt`. The executor filled in [run1.md](run1.md) and [run2.md](run2.md) from those: times from the commit timestamps, models and prompts from the exports, changes from the diff of each commit.
4. The executor checked each run branch: `dotnet build` on the .NET 10 SDK, no `System.Web`, `System.Messaging` or MSMQ references, the image tag in the registry and no secrets in the diff.

## Timings

Pending.

## Findings

Run 1, steps 1 (set up) and 2 (assess):

1. **Managed identity trap on `vm-dev01`.** The VM has a system-assigned managed identity, and `DefaultAzureCredential` tries `ManagedIdentityCredential` before `AzureCliCredential`. The app would get the VM's token, which has no data roles, and fail with 403 on Blob, Service Bus and Key Vault. Fix: set the user environment variable `AZURE_TOKEN_CREDENTIALS=AzureCliCredential` on `vm-dev01` only ([Credential chains in the Azure Identity library for .NET](https://learn.microsoft.com/dotnet/azure/sdk/authentication/credential-chains)); the deployed app still uses its managed identity. The owner kept the data roles on the user rather than giving them to the VM identity. In protocol v2, step 1.3.
2. **No git identity for a fresh VM user.** The first commit fails with `Author identity unknown`. Fix: right after `gh auth login`, set `user.name` and a GitHub no-reply `user.email` from `gh api user`, and run `gh auth setup-git` so `git push` uses the gh sign-in. In protocol v2, step 1.2.
3. **Sign-in through Bastion.** Browser sign-in inside the Bastion session is awkward, and passkeys and password managers don't work there. Fix: device code flows (`az login --use-device-code`, and `gh auth login --web` with the code entered at `github.com/login/device`), completed on the attendee's own device. Some tenants block device code flow with Conditional Access. In protocol v2, step 1.2.
4. **The assessment dashboard has no chat and no model picker.** Step 2 ran from the dashboard, so there's no chat export and the model is the extension's default. In protocol v2, recording only asks for chat exports where the owner prompted in Copilot Chat.
5. **The report targets App Service for Windows by default.** The target dropdown defaults to **Azure App Service (Windows)**; the kit targets App Service for Linux (containers) (PRD §2, B09). It has to be switched before **Create Plan**, or the plan targets Windows. The owner re-planned with Linux. In protocol v2, step 2.3; a candidate for a B10 instruction.
6. **Create Plan covers only Cloud Readiness.** **Create Plan (6)** planned the six Cloud Readiness issues: static content, SQL connection, Windows authentication (Mandatory), connection strings, local IO and MSMQ (Mandatory). It did **not** include the .NET 10 upgrade: run 1's first plan (`.github/modernize/ContosoUniversity-20260925073922/plan.md`, commit `7904c1b`) says "No … runtime/framework upgrade", so the upgrade has to be requested explicitly. That's a hot spot for protocol v2 and the B10 playbook. The plan also included **Windows AD to Microsoft Entra ID**; its open questions were Blob or Files and SQL Database or Managed Instance, which the kit answers (Blob, Managed Instance); and its integration verification defaulted to mock mode because no Azure environment was given. The owner sent a correction reply: upgrade first, remove Entra ID, Blob, Managed Instance with local SQL auth kept, order database, Blob, Service Bus, Key Vault, `DefaultAzureCredential` with no keys, App Service for Linux containers, no provisioning. In protocol v2, steps 2.5, 3.2 and 3.3.
7. **False "Windows authentication detected" (Mandatory).** The app has no user sign-in (`BaseController` uses `"System"`, and `FilterConfig` has no `AuthorizeAttribute`). The finding comes only from `<IISExpressWindowsAuthentication>enabled</IISExpressWindowsAuthentication>` (with anonymous authentication disabled) in `ContosoUniversity.csproj` and `Integrated Security=True` in the LocalDB connection string in `Web.config`. It's not applicable, but the first plan included an Entra ID migration for it. The owner removed it. A B10 instruction should say so. In protocol v2, deselect it before **Create Plan** (step 2.4) or remove it in the correction reply (step 3.2).
8. **Planning model:** the plan ran with the `modernize` agent, GPT-6 Sol at reasoning effort Medium (2026-09-25).
9. **"Projects: 1 error"** in the C# Dev Kit status bar on the legacy .NET Framework project before the upgrade is expected.
10. **Create Plan abandoned for the prompt files.** Run 1's first **Create Plan** attempt and its correction chat were stopped. The owner copied `.github/prompts` from the working branch onto `spike/b06-run1` and continued with `/modernize-plan` and `/modernize-execute`, so run 1 also uses the pinned agent and models. Reasoning effort is set in the model picker: Medium for GPT-6 Sol, maximum for GPT-6 Luna.
11. **`reasoning-effort` breaks prompt files.** A `reasoning-effort:` line in the prompt-file frontmatter stopped both prompt files from working in VS Code on `vm-dev01`. The owner removed it (`spike/b06-run1` commit `4508c6d`), and reasoning effort is set in the model picker instead: Sol Medium, Luna maximum.
12. **Slash command didn't run the prompt file** (cause in finding 14). Typing `/modernize-plan` in chat didn't work; opening the prompt file and selecting **▶ Run Prompt** (or **Chat: Run Prompt**) did. Protocol v2 makes **Run Prompt** the primary way, with the slash command as the alternative.
13. **The plan asked which Azure resources to integrate with.** `/modernize-plan` asked "What resource reference should the real-mode integration tests use for the existing Azure environment?", offering subscription and resource group, full resource IDs, or configuration keys supplied at execution time. The owner chose configuration keys, with no identifiers in the plan. The prompt files now say so, and that every resource already exists: at the event, App Service, ACR, SQL MI and the datacenter are deployed for each attendee, so the plan must never provision.
14. **The prompt-file pins may not apply with Run Prompt.** The chat footer in the owner's screenshot showed agent `Agent` and model `GPT-5.6 Sol`, while the file pins `agent: modernize` and `model: ["GPT-6 Sol (copilot)"]`. **Resolved:** the owner was in the Agent Host harness, which doesn't load prompt files. In the Local harness, `/modernize-plan` works and applies the pinned agent. This also explains finding 12. The abandoned Agent Host plan is committed on `spike/b06-run1` as evidence, and the plan was re-run in Local. Protocol v2 step 3.1 says to use the Local harness, and run 2 checks the footer.
15. **Custom assessment, not the default (owner decision).** The default **Start Assessment** defaults to App Service (Windows), which led to the wrong plan target (finding 5). C3 must be a custom assessment with the target set to App Service for Linux (containers). The reset run 1 ran it in the Local harness; the options are in protocol v2 step 2.2 and go into the B11 guides.
16. **Prompt files replaced by skills (owner decision).** The assessment dashboard switches the chat to the Copilot harness, which doesn't load prompt files, so the pinned prompt files didn't run from the natural flow. The owner replaced them with the agent skills `modernize-plan` and `modernize-execute`, which load in both harnesses. Skills can't pin an agent or model, so the owner picks `modernize`, GPT-6 Sol at Medium and GPT-6 Luna at maximum in the chat pickers.

17. **Workspace skills missing from the agent's list.** In the Copilot harness, `modernize-plan` and `modernize-execute` don't appear in the agent's "Available skills" list, which shows only the extension's built-in skills (`assessment`, `create-modernization-plan`, `implementing-code`, `runtime-validation`, `modernization-integration-tests` and others). The files are in the owner's clone. A likely cause is `disable-model-invocation: true`: per [Use Agent Skills in VS Code](https://code.visualstudio.com/docs/agent-customization/agent-skills), it stops the agent from loading a skill by itself and leaves only the `/` slash command, so the skill wouldn't be offered to the model. Whether `/modernize-plan` shows in the slash menu: pending the owner. Meanwhile the owner is running the built-in `/create-modernization-plan` with the `modernize-plan` SKILL.md body as its input. Protocol v2 step 3 is decided after that result.
18. **Dashboard as the primary path (owner decision).** The owner chose the dashboard buttons for attendees: custom assessment, **Create Plan** from the report, then execute. They write the prompts, so no skill or prompt file is needed. The risk is that each button runs with whatever agent and model are selected, so protocol v2 puts a ⚠️ check before each one. The review checklist and the inlined correction reply cover what **Create Plan** misses (findings 5–7). The workspace skills were removed.
19. **The .NET upgrade agent isn't installed (owner decision: no change).** Learn says the `modernize` agent hands .NET version upgrades to GitHub Copilot upgrade. In VS Code that's a separate extension, `ms-dotnettools.upgrade-agent` (1.1.539, preview, not deprecated), which `vscjava.migrate-java-to-azure` doesn't bundle: it depends only on Copilot Chat. That likely explains "No … runtime/framework upgrade" in the **Create Plan** output (finding 6). The owner decided to keep the kit on the modernization extension only, with the .NET 10 upgrade added through the plan review and correction reply (protocol v2 step 3.3). If the upgrade step proves unreliable, installing the upgrade extension is the option for B10 and B13.
20. **Run 1's dashboard plan still has no upgrade** (commit `e5defe5`, `.github/modernize/contoso-university-azure-services-modernization`). Even from the custom assessment with the Linux target, **Create Plan** says "No runtime/framework upgrade is included because none was requested". That matches finding 19: the upgrade agent isn't installed.
21. **Windows authentication is easy to leave selected.** The plan includes task 005, Windows AD to Entra ID, because Windows authentication wasn't deselected before **Create Plan**. Protocol v2 now has a second ⚠️ callout, as loud as the model check (step 3.2).
22. **Create Plan chains both alternatives.** It kept 001 Blob, 002 Azure Files, 003 Azure SQL Database and 004 Azure SQL Managed Instance in sequence, and put the upload task 007 on `migration-azure-storage-mount` (Azure Files). Executed as is, both alternatives would be applied. It also had no local SQL auth rule, no hosting target ("No deployment target is assumed") and no rule against keys. The correction reply (protocol v2 step 3.4) removes 002, 003 and 005, moves 007 to Blob, adds the upgrade first and orders Managed Instance, Blob, Service Bus, Key Vault, then the CVE check.
23. **The plan uses the extension's built-in skills:** `migration-azure-storage-blob`, `migration-managed-identity`, `migration-azure-sql-database`, `migration-microsoft-entra-id`, `migration-azure-keyvault-secret` and `migration-azure-servicebus`. Input for B10: the playbook can name these rather than write its own.
24. **Chat: Export Chat failed.** The JSON export didn't work, so the owner pasted the chat into `chats/run1-step3-plan.txt`. Protocol v2 allows `.txt` exports. Checked for secrets, IDs and tokens: none.
25. **Always send a fixed kit rules prompt after Create Plan (owner decision).** Instead of a review checklist and a correction reply with task IDs, attendees always send one fixed prompt straight after **Create Plan**: seven rules (.NET 10 first, no Entra sign-in, Blob and Managed Instance only, local SQL auth kept, the order, `DefaultAzureCredential` with no keys and endpoints from configuration, App Service for Linux with no provisioning) ending with a self-check table, one row per rule. The check is that all seven rows are met, otherwise send it again. It's in protocol v2 step 3.4; the owner is using it in run 1.
26. **A finished plan can't be corrected (key finding).** When the owner sent the kit rules prompt after **Create Plan**, the `modernize` agent replied: "I can't revise the artifacts in this session because the planning coordinator has already completed, and this modernization workflow does not permit re-invoking the same coordinator or manually editing its outputs... Start a new planning session with these seven rules as the planning input." So no follow-up prompt can fix a finished plan, and findings 21, 22 and 25 can't be solved after the fact. Owner decision: skip **Create Plan**; after the custom assessment, start a new chat (`modernize`, GPT-6 Sol, Medium) and send `/create-modernization-plan` with the seven kit rules as the planning input and the 1–7 self-check table at the end. A wrong plan is deleted and regenerated. The owner discarded the **Create Plan** output in a commit on `spike/b06-run1`. In protocol v2, step 3.
27. **The rules-driven plan met every rule** (commit `39445e8`, `.github/modernize/contoso-university-dotnet10-azure`; the discarded **Create Plan** output is commit `f467e72`, the evidence for finding 26). `tasks.json` itself follows all seven rules, in a straight dependency chain: 001 upgrade to `net10.0` (type `upgrade`, no skill attached), 002 SQL Managed Instance (`migration-azure-sql-database`, local SQL auth kept), 003 Blob (`migration-azure-storage-blob`, no Files or mounts), 004 Service Bus, 005 Key Vault, 006 CVE fixes. No Entra ID task, no provisioning, `DefaultAzureCredential` with the five configuration keys, App Service for Linux (containers). The chat's rules table matches. One plan generation was enough. Watch point: task 001 has no skill, and GitHub Copilot upgrade isn't installed (finding 19), so how the `modernize` agent carries out the upgrade is recorded with the execution results. The chat export `chats/run1-step3-plan-v2.txt` has no secrets, IDs or tokens.
28. **Execution blocked by stale session state.** "Execute the plan", sent in the planning chat with `modernize` and GPT-6 Luna at maximum, handed off to `execution-coordinator`, which returned **BLOCKED** before any task: "the coordinator saw an in-progress execution-phase tracker. No files were changed and no build or tests were run." It offered "I'll fix manually", "retry from scratch" and "abort". There's no tracker file under `.github/modernize` or `app/ContosoUniversity/.github`, so it's likely session state left by the abandoned attempts in the same VS Code window. The owner then tried "retry from scratch" in the same chat, which was blocked too: "Maximum sub-agent depth of 4 reached. Complete this task without spawning further sub-agents." No tasks ran and nothing changed. Nested coordinators (`modernize` → `create-modernization-plan` → `execution-coordinator` → retry) use up the depth budget within one chat. The owner aborted, reloaded the window and started execution in a new chat (`modernize`, GPT-6 Luna at maximum): "Execute the modernization plan in .github/modernize/contoso-university-dotnet10-azure, starting with task 001." Protocol v2 rule (step 3.4): plan and execute in separate chats, never retry execution in the planning chat, and reload the window after any blocked execution. Input for B10.
29. **The agent switches branches per task and can strand uncommitted work (important).** Task 001, the .NET 10 upgrade, ran and built ("Task 001 is implemented and the solution build passes"), but the owner hadn't committed it. When task 002 started, the `modernize` agent's version control tool ran `prepareBranch` and switched `spike/b06-run1` to `appmod/dotnet-migration-azure-sql-database-<timestamp>`. Afterwards the working tree was back at .NET Framework 4.8 (legacy csproj, no `Program.cs` or `appsettings.json`), and the tool reported "no restore-stash action", so task 001's output was probably stashed. The agent paused task 002 correctly. It also writes per-task artifacts to `.github/modernize/code-migration/<timestamp>/{plan.md,progress.md}`. Recovery: task 001 was restored from the agent's auto-stash and pushed to `spike/b06-run1` as `0dccd8e`. Protocol v2 rules (step 3.4): commit and push after every task before the next one starts; expect `appmod/*` branches per task, decline branch creation if offered, and merge or fast-forward back so the final result is on `spike/b06-runN`. Input for B10 and the B11 guides.
30. **Task 001, the .NET 10 upgrade, without the upgrade agent** (commit `0dccd8e`). The `modernize` agent did it with no skill attached (finding 19): an SDK-style csproj, `Program.cs` with `AddDbContext<SchoolContext>` reading `ConnectionStrings:DefaultConnection` from configuration (and throwing if it's missing), `appsettings.json` and `appsettings.Development.json`, and the documented `dotnet user-secrets` route. It deleted `Global.asax`, `Global.asax.cs`, `Web.config`, `Views/Web.config` and `packages.config`, so part of the step 5 Global.asax gap was done in the upgrade. The chat reported "Task 001 is implemented and the solution build passes"; the executor's own build check comes with the run branch check. The diff has no passwords, keys or IDs.
31. **Task 002, SQL Managed Instance, in a new chat.** It completed: it removed the checked-in LocalDB connection string, updated `README.md` and `SETUP_TESTING_GUIDE.md`, and left `Program.cs`, the EF model and the packages unchanged. The Release build passed; its CVE, consistency and completeness checks passed; it found 0 tests and ran no live database test. The tool committed it itself (`a5393db`) on a new branch, `appmod/dotnet-migration-azure-sql-database-<timestamp>` (`prepareBranch` and `commitChanges` per task), which the owner fast-forwarded into `spike/b06-run1`. Per-task notes land in `.github/modernize/code-migration/<timestamp>/{plan.md,progress.md,summary.md}`. Protocol v2 step 3.4 is now one task per new chat, then fast-forward and push.
32. **First local run on .NET 10 works; the next task hit a stale tracker.** After tasks 001 and 002 (`a5393db`, fast-forwarded onto `spike/b06-run1`), the owner ran the .NET 10 app on `vm-dev01` against `10.10.1.4` with SQL authentication from user secrets: the home, Students, Courses, Instructors and Departments pages all worked. Task 003 (Blob), in a new chat, then stopped on a leftover "in-progress invocation tracking item" from task 002's run. The agent marked it complete and went on when told to. Protocol v2 step 3.4 now reloads the window after each task, before the next new chat, and says what to reply if a stale tracker shows up.
33. **Task 003, Blob, done, and branch behaviour differs by path.** After the stale-tracker retry, task 003 ran directly on `spike/b06-run1`: no `prepareBranch`, no `appmod/*` branch and no commit ("I did not commit"), unlike task 002. It implemented upload, download, replacement and deletion with `BlobServiceClient` and `DefaultAzureCredential` from the `Storage:*` configuration, streamed images through the app (`Courses/TeachingMaterialImage`, private container), rewrote `TEACHING_MATERIAL_UPLOAD.md` and `README.md`, and removed the uploads `.gitkeep`. The Release build passed with 0 warnings and 0 errors; no live Blob test in the task. **Live check passed (requirement 12, upload):** the owner uploaded an image through **Courses** > **Edit** on `vm-dev01`, and the blob `course_1045_<guid>.png` (`image/png`, 1403 bytes) landed in `teaching-materials` over the private endpoint, with `DefaultAzureCredential` narrowed to `AzureCliCredential` by `AZURE_TOKEN_CREDENTIALS`. Protocol v2 step 3.4.3 now checks `git status --short --branch` after every task: fast-forward from `appmod/*`, or commit, then push.
The rest is pending run 1.

**False start and reset.** Findings 4–14 come from run 1's false start: the assessment and **Create Plan** in the Agent Host harness, then prompt files that Agent Host didn't load. The owner then reset run 1 (commit `run1: reset for a fresh start in the Local harness`, removing the plan folders and `assessment/run1`) and redid steps 2 and 3 in the Local harness. Run 1's step times count from that reset commit.

Note: the owner's hand-run assessment from 2026-09-24 was lost with the old clone before run 1, so it isn't in [assessment/](assessment/). Run 1 started from a fresh clone.

## Decisions

- **Custom assessment, then a rules-driven plan (owner decision, 2026-09-25).** Step 2 runs a custom assessment targeting App Service for Linux (containers). Step 3 skips **Create Plan**: a new chat with `modernize` and GPT-6 Sol at Medium sends `/create-modernization-plan` with the seven kit rules as the planning input, the plan is checked against the rules table and `tasks.json`, and a wrong plan is deleted and regenerated. Execution then runs with GPT-6 Luna at maximum. A ⚠️ callout before each chat says which agent and model to select, because the chat uses whatever is selected. Prompt files, agent skills, **Create Plan** with a correction reply and a follow-up kit rules prompt were tried first and dropped (findings 10–26). [B06](../../backlog/B06-spike-ghcp.md) requirement 7 still leaves the model unpinned; it now says the protocol names what to select.

## Evidence

- [assessment/](assessment/): the saved assessment reports per run.
- [pe-probe.md](pe-probe.md): the private-endpoint probe from `vm-dev01` and the deployed settings.
- [run1.md](run1.md) and [run2.md](run2.md): the results sheets, filled in by the executor from the run branches.
- `chats/` and `runN-versions.txt` on the run branches: the chat exports per step (checked for secrets) and the tool versions.

## Follow-ups

- **B10: a skill that wraps `create-modernization-plan` with the kit rules.** The planning prompt in protocol v2 step 3.2 is the candidate: it has to be the planning input, because a finished plan can't be corrected (finding 26). Check first that a workspace skill shows in the slash menu in the Copilot harness (finding 17).
- **B10: playbook around the dashboard.** The golden path uses the dashboard buttons, so the playbook needs the pre-click model check, and the planning prompt with the kit rules and its self-check table (protocol v2 step 3). If B10 ships skills, note findings 16 and 17: workspace skills with `disable-model-invocation: true` didn't show in the Copilot harness's skill list, and skills can't pin a model. Prompt files don't load in the Copilot (Agent Host) harness.
- **B10 and B13: the .NET upgrade agent.** If the upgrade through the correction reply proves unreliable, consider adding `ms-dotnettools.upgrade-agent` to the dev VM (finding 19).
- **B10 and B11: the agent's per-task branches.** The playbook and guides must say to commit and push after every task and to bring `appmod/*` branches back to the working branch (finding 29).
- **B11 attendee guides:** sign in on `vm-dev01` with device code flows (`az login --use-device-code`, `gh auth login --web` with the code at `github.com/login/device`), completed on the attendee's own device, and say that some tenants block device code flow with Conditional Access. After `az login`, the attendee picks their own workload subscription (the one that holds `rg-datacenter`) in the subscription picker and checks it with `az account show --query name -o tsv`; guides never hard-code a subscription. Right after `gh auth login`, set the git identity from the GitHub account with the no-reply email (`<id>+<login>@users.noreply.github.com`) and run `gh auth setup-git`, because a fresh VM user has no git identity and the first commit fails with `Author identity unknown`. Run C3 as a custom assessment targeting App Service for Linux (containers), with the options in protocol v2 step 2.2. Before each dashboard button that starts a chat, check the agent and model in the Chat panel pickers, because the button uses whatever is selected.

The rest is pending the runs.
